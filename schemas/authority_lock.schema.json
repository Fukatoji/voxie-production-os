{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Voxie Authority Content Lock",
  "type": "object",
  "required": [
    "lock_version",
    "lock_id",
    "record_type",
    "index_id",
    "index_path",
    "index_sha256",
    "algorithm",
    "entry_count",
    "entries"
  ],
  "properties": {
    "lock_version": {
      "const": "1.0"
    },
    "lock_id": {
      "type": "string",
      "pattern": "^VOS-AUTHORITY-CONTENT-LOCK-V[0-9]{2}$"
    },
    "record_type": {
      "const": "authority_lock"
    },
    "index_id": {
      "type": "string",
      "pattern": "^VOS-AUTHORITY-INDEX-V[0-9]{2}$"
    },
    "index_path": {
      "type": "string",
      "pattern": "^manifests/control/authority-index-v[0-9]{2}\\.yaml$"
    },
    "index_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "algorithm": {
      "const": "sha256"
    },
    "entry_count": {
      "type": "integer",
      "minimum": 1
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["authority_id", "path", "sha256"],
        "properties": {
          "authority_id": {
            "type": "string",
            "pattern": "^[A-Z0-9_-]+$"
          },
          "path": {
            "type": "string",
            "pattern": "^(manifests|workflows|config)/[^\\s]+$"
          },
          "sha256": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
